# MS8100 Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rebuild `/products/ms8100` to match the supplied mobile design using the supplied production artwork.

**Architecture:** Keep the route and shared `Layout` unchanged. Replace the current monolithic, chart-generated page with a static data-driven React page and colocated descriptive image assets; use raster artwork for the scientific visuals and Tailwind for layout and typography.

**Tech Stack:** React 19, TypeScript 5.8, Tailwind CSS v4, motion/react, react-router-dom v7.

## Global Constraints

- Modify only MS8100-specific source/assets plus this documentation.
- Preserve the shared Header/Footer and inquiry route `/inquiry?product=MS8100`.
- Use relative imports and Tailwind utility classes only.
- Keep Simplified Chinese user-facing text.
- Do not introduce new dependencies or generated charts.

---

### Task 1: Import and map design assets

**Files:**
- Create: `src/assets/ms8100/details/*.png`
- Test: PowerShell asset manifest assertion

**Interfaces:**
- Consumes: nine supplied PNG files from `C:\Users\lengjing\Desktop\文档\官网\3`.
- Produces: descriptive, stable import paths under `src/assets/ms8100/details/`.

- [ ] **Step 1: Run a failing asset-manifest assertion**

Run a PowerShell assertion for the nine target filenames and confirm it exits non-zero because the directory is absent.

- [ ] **Step 2: Copy assets with descriptive names**

Create the details directory and copy the files one-to-one using `Copy-Item -LiteralPath`.

- [ ] **Step 3: Re-run the manifest assertion**

Confirm all nine files exist and are non-empty.

### Task 2: Rebuild the page content

**Files:**
- Modify: `src/pages/products/MS8100Details.tsx`
- Test: PowerShell source-contract assertion

**Interfaces:**
- Consumes: the nine descriptive image imports and existing hero `bg.png`.
- Produces: named `MS8100Details()` route component with hero, quadrupole, ion source, highlights, stability, comparison, and MassNova sections.

- [ ] **Step 1: Run a failing source-contract assertion**

Assert that the page contains all seven intended section labels, contains no `recharts`, and references all new asset imports. Confirm the old page fails this check.

- [ ] **Step 2: Replace the page implementation**

Use static arrays for bullet lists and highlight cards, semantic sections and headings, responsive Tailwind sizing, image aspect preservation, and a working inquiry button.

- [ ] **Step 3: Re-run the source-contract assertion**

Confirm every required label/import is present and `recharts` is absent.

### Task 3: Compile and visually verify

**Files:**
- Verify: `src/pages/products/MS8100Details.tsx`
- Verify: `src/assets/ms8100/details/*.png`

**Interfaces:**
- Consumes: the rebuilt route.
- Produces: a type-safe production build and visually checked mobile layout.

- [ ] **Step 1: Run type checking**

Run `npm run lint`; expected exit code 0.

- [ ] **Step 2: Run production build**

Run `npm run build`; expected exit code 0.

- [ ] **Step 3: Inspect the page at mobile width**

Launch the local server, open `/products/ms8100`, capture the full mobile page, and compare section order, overflow, image proportions, and spacing with the supplied design.

- [ ] **Step 4: Review the final diff**

Confirm no unrelated tracked files were changed and retain the user's pre-existing working-tree changes.
